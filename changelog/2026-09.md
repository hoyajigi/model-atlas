# Changelog — 2026-09

- 2026-09-25 · New models added from HF trending: DeepSeek-V4.1-Flash (DeepSeek), Qwen3.8-Flash-Next (Qwen), MiMo-V2.6-Pro-RL and MiMo-V2.6-Flash-RL (Xiaomi, new org), AliceAI-Foundation-80B-A3B-Base (Yandex, new org), Qwen-Image-2.1 (Qwen), Krea-2-Turbo (Krea), SD 3.5 Medium (Stability AI). Source: https://huggingface.co/api/models?sort=trendingScore.
- 2026-09-25 · Pricing corrections (models.dev/LiteLLM sync flagged 20 mismatches; verified against official docs):
  - DeepSeek renamed its Flash model to `deepseek-flash`; official peak-hours pricing dropped from $0.44/$1.32 to $0.30/$1.20 per Mtok (cache-hit $0.006). Applied to `deepseek-v4-flash` and the legacy aliases `deepseek-chat`, `deepseek-reasoner`, `deepseek-v4-flash-vision-exp` (all now marked `deprecated` — DeepSeek's pricing page says these names redirect to Flash).
  - Fireworks AI DeepSeek V4 Flash (`deepseek-v4-flash-0731` and undated alias): $0.14/$0.28 -> $0.22/$0.66 per Mtok, matching Fireworks' official Standard serverless tier.
  - Fireworks AI DeepSeek V4 Pro (undated alias): $1.74/$3.48 -> $1.32/$3.96 per Mtok, matching the already-correct `-0813` alias and Fireworks' Standard tier.
  - OpenAI `gpt-5.6` and `gpt-5.6-sol`: $5/$30 -> $4/$20 per Mtok (cached input $0.40), matching OpenAI's official standard tier.
  - Together AI Qwen3.7-Max: $1.25/$3.75 -> $1.50/$4.50 per Mtok (cached input $0.30), matching Together's official pricing page.
  - Mistral Ministral 3B: $0.04/$0.04 -> $0.10/$0.10 per Mtok.
  - Mistral Voxtral Small: output $0.3 -> $0.4 per Mtok (input unchanged at $0.1).
- 2026-09-25 · Correction: Mistral's `open-mistral-nemo` status field said `deprecated` but its own source note already documented a 2026-07-31 retirement — status field corrected to `retired` to match.
- 2026-09-25 · Filled missing t2i pricing: Google `gemini-3-pro-image` ($0.134/image, matching the `-preview` alias), `gemini-3.1-flash-image` ($0.067/image, matching the `-preview` alias), and `gemini-3.1-flash-lite-image` ($0.0336/image) — all previously missing a `pricing.image` block despite their preview/sibling aliases having one.
- 2026-09-25 · t2i pricing verified unchanged against official pages: fal.ai (5 models), Replicate FLUX flat-rate models (3), xAI Grok Imagine (3, including the previously-added `grok-imagine-image-quality`), OpenAI gpt-image family (per-Mtok rows cross-checked; `gpt-image-2`'s two input rates — $5 text-prompt / $8 image-input — were initially misread as a mismatch but the tracked $5 correctly reflects the text-prompt rate).
- 2026-09-25 · Known upstream discrepancies not fixed (flagging instead of guessing):
  - Google Gemini image models (`gemini-2.5-flash-image`, `gemini-3-pro-image[-preview]`, `gemini-3.1-flash-image[-preview]`, `gemini-3.1-flash-lite-image`, `gemini-omni-flash-preview`): LiteLLM's flagged `output_per_mtok` values are Google's *text*-output token rate; our tracked values are Google's *image/video*-output token rate (both are published on Google's pricing page as separate rows for the same model — not an error).
  - Fireworks AI `deepseek-v4-pro` (undated alias): LiteLLM shows $1.20/$1.20, which matches neither Fireworks' Standard ($1.32/$3.96) nor Priority ($1.65/$4.95) tier — looks like a stale LiteLLM entry.
  - DeepSeek `deepseek-chat`/`deepseek-reasoner`: LiteLLM shows $0.28/$0.42, which doesn't match any rate on DeepSeek's current pricing page (legacy names now redirect to Flash pricing) — looks like a stale LiteLLM entry.
  - Mistral `magistral-medium-latest`: not listed on Mistral's current pricing page at all (only "Mistral Medium 3.5" is); LiteLLM's flagged $1.50/$7.50 happens to equal Mistral Medium 3.5's price, suggesting a possible alias mix-up upstream — left unchanged since no official rate for this exact model could be confirmed.
  - Mistral `open-mistral-nemo`: retired 2026-07-31 with no live pricing; LiteLLM's $0.30/$0.30 doesn't correspond to any current published rate — pricing left at last-known value per the existing note.
  - Stability AI's pricing page (`platform.stability.ai/pricing`) is client-side rendered and couldn't be re-fetched this run; the 5 tracked Stable Image / SD 3.5 offerings were left unchanged (last verified 2026-07-20).
  - Google's `deep-research-preview-04-2026` and `deep-research-max-preview-04-2026` offerings are tagged `modality = "text-to-image"` in our data (inherited from models.dev), which looks like a mislabel for a text/agent research model — not corrected here since it's a modality classification issue, not a pricing one.
