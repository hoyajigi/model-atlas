name: Curator agent

on:
  schedule:
    - cron: '20 22 * * 1,4' # 07:20 KST Mon/Thu
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  curate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          # Pass a token explicitly: without one the action falls back to the
          # GitHub App path and requests an OIDC token, which needs
          # `id-token: write` and fails the job before Claude ever starts.
          #
          # Prefer CURATOR_PAT. GitHub refuses to trigger workflows for events
          # created with GITHUB_TOKEN, so a PR opened with it gets no CI run
          # (see PR #2, which sat a month with an unapproved check). A PAT opens
          # the PR under a real account and CI runs normally. Falls back to
          # GITHUB_TOKEN so the job still works before the PAT exists.
          github_token: ${{ secrets.CURATOR_PAT || secrets.GITHUB_TOKEN }}
          prompt: /curate
          # The curator edits TOML/seeds, runs npm scripts, and opens a PR.
          # Without --allowedTools the action denies those calls and the job
          # goes green having changed nothing (11 denials on run 32567797248).
          # 200 turns: a full four-task pass took 157 (run 32567981855) and the
          # action hard-fails the job when the real count exceeds the limit.
          claude_args: >-
            --max-turns 200
            --allowedTools "Read,Glob,Grep,Edit,Write,WebFetch,TodoWrite,Bash(npm:*),Bash(git:*),Bash(gh:*),Bash(node:*)"
